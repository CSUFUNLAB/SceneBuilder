After node $event_node_id$ fails, how does the throughput of data flow $target_data_flow_id$ change relative to before the event (increase, unchanged, or decrease)? ||| [increase, unchanged, decrease]
After node $event_node_id$ fails, how does the packet loss count of data flow $target_data_flow_id$ change relative to before the event (increase, unchanged, or decrease)? ||| [increase, unchanged, decrease]
After node $event_node_id$ recovers, how does the throughput of data flow $target_data_flow_id$ change relative to before the event (increase, unchanged, or decrease)? ||| [increase, unchanged, decrease]
After node $event_node_id$ recovers, how does the average delay of data flow $target_data_flow_id$ change relative to before the event (increase, unchanged, or decrease)? ||| [increase, unchanged, decrease]
After channel $event_channel_id$ fails, how does the throughput of data flow $target_data_flow_id$ change relative to before the event (increase, unchanged, or decrease)? ||| [increase, unchanged, decrease]
After channel $event_channel_id$ fails, how does the packet loss count of data flow $target_data_flow_id$ change relative to before the event (increase, unchanged, or decrease)? ||| [increase, unchanged, decrease]
After channel $event_channel_id$ recovers, how does the throughput of data flow $target_data_flow_id$ change relative to before the event (increase, unchanged, or decrease)? ||| [increase, unchanged, decrease]
After channel $event_channel_id$ recovers, how does the average delay of data flow $target_data_flow_id$ change relative to before the event (increase, unchanged, or decrease)? ||| [increase, unchanged, decrease]
After interface $event_nic_id$ fails, how does the throughput of data flow $target_data_flow_id$ change relative to before the event (increase, unchanged, or decrease)? ||| [increase, unchanged, decrease]
After interface $event_nic_id$ fails, how does the packet loss count of data flow $target_data_flow_id$ change relative to before the event (increase, unchanged, or decrease)? ||| [increase, unchanged, decrease]
After interface $event_nic_id$ recovers, how does the throughput of data flow $target_data_flow_id$ change relative to before the event (increase, unchanged, or decrease)? ||| [increase, unchanged, decrease]
After interface $event_nic_id$ recovers, how does the average delay of data flow $target_data_flow_id$ change relative to before the event (increase, unchanged, or decrease)? ||| [increase, unchanged, decrease]
After the load of data flow $event_data_flow_id$ increases, how does the available bandwidth of channel $target_channel_id$ change relative to before the event (increase, unchanged, or decrease)? ||| [increase, unchanged, decrease]
After the load of data flow $event_data_flow_id$ increases, how does its average delay change relative to before the event (increase, unchanged, or decrease)? ||| [increase, unchanged, decrease]
After the load of data flow $event_data_flow_id$ decreases, how does the available bandwidth of channel $target_channel_id$ change relative to before the event (increase, unchanged, or decrease)? ||| [increase, unchanged, decrease]
After the load of data flow $event_data_flow_id$ decreases, how does its average delay change relative to before the event (increase, unchanged, or decrease)? ||| [increase, unchanged, decrease]
After node $event_node_id$ fails, which status transition occurs for node $target_node_id$ relative to before the event (recovered, unchanged, degraded, failed, or saturated)? ||| [recovered, unchanged, degraded, failed, saturated]
After node $event_node_id$ recovers, which status transition occurs for node $target_node_id$ relative to before the event (recovered, unchanged, degraded, failed, or saturated)? ||| [recovered, unchanged, degraded, failed, saturated]
After channel $event_channel_id$ fails, which status transition occurs for channel $target_channel_id$ relative to before the event (recovered, unchanged, failed, or saturated)? ||| [recovered, unchanged, failed, saturated]
After channel $event_channel_id$ recovers, which status transition occurs for channel $target_channel_id$ relative to before the event (recovered, unchanged, failed, or saturated)? ||| [recovered, unchanged, failed, saturated]
After interface $event_nic_id$ fails, which status transition occurs for interface $target_nic_id$ relative to before the event (recovered, unchanged, degraded, failed, or saturated)? ||| [recovered, unchanged, degraded, failed, saturated]
After interface $event_nic_id$ recovers, which status transition occurs for interface $target_nic_id$ relative to before the event (recovered, unchanged, degraded, failed, or saturated)? ||| [recovered, unchanged, degraded, failed, saturated]
After the load of data flow $event_data_flow_id$ increases, which status transition occurs for channel $target_channel_id$ relative to before the event (recovered, unchanged, failed, or saturated)? ||| [recovered, unchanged, failed, saturated]
After the load of data flow $event_data_flow_id$ decreases, which status transition occurs for channel $target_channel_id$ relative to before the event (recovered, unchanged, failed, or saturated)? ||| [recovered, unchanged, failed, saturated]
After the load of data flow $event_data_flow_id$ increases, which status transition occurs for interface $target_nic_id$ relative to before the event (recovered, unchanged, degraded, failed, or saturated)? ||| [recovered, unchanged, degraded, failed, saturated]
After the load of data flow $event_data_flow_id$ decreases, which status transition occurs for interface $target_nic_id$ relative to before the event (recovered, unchanged, degraded, failed, or saturated)? ||| [recovered, unchanged, degraded, failed, saturated]
